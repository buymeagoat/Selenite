--extra-index-url https://download.pytorch.org/whl/cpu
# For CUDA builds, override the extra-index-url to the matching CUDA wheel URL and bump
# torch/torchaudio/torchvision accordingly. CPU wheels are the baseline/default.

# Core API
fastapi==0.124.2
uvicorn[standard]==0.38.0
sqlalchemy==2.0.45
alembic==1.17.2
pydantic==2.12.5
pydantic-settings==2.12.0
email-validator==2.3.0
python-multipart==0.0.20
python-json-logger==4.0.0
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
python-docx==1.2.0
aiofiles==25.1.0
aiosqlite==0.21.0
requests==2.32.5
httpx==0.28.1
psutil==7.1.3
numpy==1.26.4
python-magic==0.4.27
python-magic-bin==0.4.14; platform_system == "Windows"
ffmpeg-python==0.2.0
soundfile==0.13.1
openai-whisper==20250625

# Foundation DL stack (CPU by default; swap to CUDA wheels if available)
torch==2.2.2+cpu
torchaudio==2.2.2+cpu
torchvision==0.17.2+cpu

# ASR providers
ctranslate2==4.4.0          # for faster-whisper backend
faster-whisper==1.0.3
transformers==4.44.2        # wav2vec2 / XLS-R / WavLM
vosk==0.3.45                # Kaldi/Vosk runtime
coqui-stt==1.4.0; platform_system != "Windows"   # Coqui STT (DeepSpeech fork; Windows wheels not available)
nemo-toolkit==1.23.0; platform_system != "Windows"   # NeMo Conformer-CTC & diarization (skipped on Windows: triton wheels unavailable)

# Diarization stack
pyannote.audio==3.1.1
speechbrain==0.5.16
resemblyzer==0.1.2; platform_system != "Windows"
webrtcvad==2.0.10; platform_system != "Windows"

# Tooling / tests
pytest==9.0.2
pytest-asyncio==1.3.0
exceptiongroup==1.3.1
tomli==2.3.0
backports-asyncio-runner==1.2.0
pytest-cov==5.0.0
black==25.12.0
ruff==0.14.8
setuptools==80.9.0
